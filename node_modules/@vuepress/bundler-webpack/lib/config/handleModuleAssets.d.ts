import type * as Config from 'webpack-chain';
import type { App } from '@vuepress/core';
/**
 * Set webpack config to handle assets files
 */
export declare const handleModuleAssets: ({ app, config, }: {
    app: App;
    config: Config;
}) => void;
